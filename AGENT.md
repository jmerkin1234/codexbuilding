# Agent Notes

This repository is built under the following constraints.

## Project Goal

Build a portable custom billiards physics engine in pure C# that can run standalone and be adapted to Godot 4.6 without locking gameplay to Godot.

## Hard Constraints

- Do not reuse old project gameplay files unless the user explicitly names them.
- Keep the physics engine portable and renderer-agnostic.
- Godot 4.6 is an adapter and viewer, not the physics authority.
- The Blender scene currently open through MCP is the source for hardcoded table geometry and source object names.
- Keep object names used from Blender exactly documented in `MODEL_REFERENCE.md`.
- Maintain `README.md`, `MODEL_REFERENCE.md`, `HARDCODE_REFERENCE.md`, `PLAN.md`, and `AGENT.md` as the code evolves.
- Push to GitHub incrementally as milestones land.
- Do not jump ahead in implementation order.

## Ordered Execution

1. Extract and document Blender measurements and names.
2. Hardcode the table specification in portable C#.
3. Build the deterministic fixed-step simulation shell.
4. Implement cue strike and spin.
5. Implement rolling, sliding, and cloth friction.
6. Implement ball-ball collisions.
7. Implement cushion, jaw, and pocket interactions.
8. Implement shot event capture and settle detection.
9. Add standalone deterministic tests.
10. Add the Godot 4.6 adapter.
11. Add full 8-ball rules and game flow.

## Naming

- Preserve Blender names in documentation exactly.
- Use explicit mappings in code when a cleaner internal identifier is needed.
- Current verified six-rail names:
  `rail_head`, `rail_foot`, `rail_upper_left`, `rail_upper_right`, `rail_bottom_left`, `rail_bottom_right`

## Current Scope

The repository now has a working portable core scaffold, a fixed-step simulation shell with explicit shot phases, cue-strike input seeding for velocity and spin, a cloth-motion layer with skid-to-roll matching plus modest side-spin drift/scrub, equal-mass ball-ball collision resolution with tangential spin transfer plus controlled follow/draw carry-through, hardcoded cushion and pocket-jaw boundary interaction with angle-aware rail rebound tuning plus a separate rail-english transfer term, a mouth/drop-based pocket capture model derived from hardcoded jaw geometry with an added slow-speed lip-hang rejection pass, deterministic replay trace capture with a locked straight-shot regression signature, a standard 8-ball opening rack factory, a portable 8-ball rules layer, a portable training-mode rules path, and a Godot 4.6 adapter that records shot traces, resolves them through the portable rules engines, exposes rules/training state in framed HUD panels, renders predictive guide lines from cloned portable simulations, can now show the hardcoded table geometry as a toggleable overlay with per-layer visibility controls, boots into an orthographic top-down main camera while still supporting switchable camera presets with runtime zoom for inspection, directly instantiates the checked-in Blender table asset at `res://art/customtable_9ft.blend` when present, now binds directly to the imported Blender balls and cue stick instead of procedural fallback ball visuals, now mirrors visible ball roll from core motion plus side-spin yaw so play no longer reads as sliding decals, now uses a cleaned Godot project copy of the Blender table with `Tableslate` custom normals cleared plus tangent-sensitive meshes triangulated for better imported shading, shows transient shot-feedback banners for key events and rule outcomes, highlights current turn state in a dedicated color-accented header, keeps a dedicated last-shot summary panel for completed eight-ball and training/freeplay shots, marks the selected training/freeplay ball with a pulsing in-world ring during placement, now exposes live debug tuning for key portable-physics constants including ball follow/draw carry, glancing-rail restitution, tangential-rail retention, and rail-english transfer, uses a cleaner card-based HUD with a dedicated shot-setup panel and toggleable controls/help panel, now lets `F7` hide the gameplay HUD cards and shot banner without affecting the menu overlay, now opens the playable window at `1920x1080` by default, opens on a proper start/menu overlay with button-based mode selection and pause/menu actions, clamps player shot setup to `0.3-5.0 m/s` for regular play and `0.3-8.0 m/s` for the eight-ball break, now uses the corrected `pocket_TL1` top-left and `pocket_BM3` bottom-middle pocket references across code and docs, now keeps overlap correction from emitting fake rail or ball-contact events into the rules layer, now resolves simultaneous cue-ball contacts in a stable sorted order, and now runs Player 2 as a simple computer opponent in 8-ball with separate regular-shot and break-shot speed samples plus a turn-failover path that hands the opponent ball in hand instead of soft-locking. The next implementation step remains broader pocket-behavior and overall feel tuning.

## Progress Log

- 2026-03-14: Step 9 completed. Added `SimulationReplayTrace`, `SimulationReplayFrame`, `SimulationReplayRunner`, and `SimulationFingerprintBuilder`; locked the canonical straight-shot replay signature; verified standalone coverage at `27/27` passing tests with `dotnet test`.
- 2026-03-14: Step 10 completed. Added `StandardEightBallRack`, expanded standalone coverage to `30/30` passing tests, and replaced the Godot stub with a real adapter that builds a named scene graph, supports keyboard shot control, syncs ball visuals from `SimulationWorld`, and falls back to a procedural table when no imported scene is present. Verified with `dotnet test` and `dotnet build`; Godot runtime launch was not executed here because no Godot CLI/editor binary is available in this environment.
- 2026-03-14: Step 11 completed. Added a portable `Rules` layer with replay-trace shot summaries, 8-ball turn resolution, foul and break handling, ball-in-hand, configurable 8-ball-on-break behavior, and a separate training-mode state path. Expanded standalone coverage to `38/38` passing tests and reverified the Godot adapter build with `dotnet build`.
- 2026-03-14: Step 12 completed. Wired the Godot adapter into the portable rules engines by capturing live replay traces, resolving 8-ball and training-mode shots in `Main.cs`, exposing mode switching and cue-ball-in-hand placement controls, and surfacing rules state in the HUD. Reverified with `dotnet build` for the Godot adapter and `dotnet test` for `38/38` passing standalone tests.
- 2026-03-14: Step 13 completed. Expanded practice presentation in the Godot adapter with predictive cue/object guide lines driven by cloned portable simulations, plus freeplay ball selection and placement controls for training layouts. Reverified with `dotnet build` for the adapter and `dotnet test` for `38/38` passing standalone tests.
- 2026-03-14: Step 14 started. Added a Godot-side hardcoded-table overlay sourced from `TableSpec` for cloth bounds, cushions, jaws, pockets, and spawn/reference spots, with a runtime toggle. Reverified with `dotnet build` for the adapter and `dotnet test` for `38/38` passing standalone tests.
- 2026-03-14: Step 14 continued. Added a Godot debug mode that surfaces live portable-engine data in a second HUD panel and forces the hardcoded-table overlay visible for inspection. Reverified with `dotnet build` for the adapter and `dotnet test` for `38/38` passing standalone tests.
- 2026-03-14: Verification pass. Fixed a Godot adapter startup bug in `Main.BuildTableVisual()` by parenting procedural rail visuals before calling `LookAt()`. Reverified with `dotnet test` for `38/38` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Training-mode simplification. Removed the temporary preset/drill layer and kept training as freeplay only. Reverified with `dotnet test` for `38/38` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Step 14 continued. Split the hardcoded-table overlay into cloth, cushion, jaw, pocket, and spot sublayers with runtime hotkeys, and surfaced the live layer state in both the HUD and debug panel. Reverified with `dotnet test` for `38/38` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Step 14 continued. Added switchable Godot camera presets and runtime zoom controls for broadcast, top-down, foot-rail, and side-rail inspection views, with the active camera state reflected in the HUD and debug panel. Reverified with `dotnet test` for `38/38` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Step 14 continued. Reworked the Godot HUD into styled status and debug panels with wrapped text so the live state readout is more usable during play and inspection. Reverified with `dotnet test` for `38/38` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Step 14 continued. Added a transient Godot shot-feedback banner that reacts to shot starts, first contact, pocketed balls, scratches, fouls, wins, and other rule outcomes without changing the portable core. Reverified with `dotnet test` for `38/38` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Step 14 continued. Added a color-accented status header in the Godot HUD so training state, active player, winner state, and ball-in-hand stand out immediately instead of being buried in the body text. Reverified with `dotnet test` for `38/38` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Step 14 continued. Changed the Godot adapter so the main/default camera preset is now an orthographic top-down view, while the broadcast, foot-rail, and side-rail presets remain available through runtime cycling and zoom. Reverified with `dotnet test` for `38/38` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Step 14 continued. Added a dedicated Godot last-shot summary panel that turns completed eight-ball and training/freeplay rule results into readable shot breakdowns without changing the portable core. Reverified with `dotnet test` for `38/38` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Step 14 continued. Replaced the weak training selection scale bump with a pulsing in-world ring marker so the selected freeplay/layout ball is obvious during placement. Reverified with `dotnet test` for `38/38` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Step 14 continued. Switched the Godot adapter from the placeholder imported-table scene path to the checked-in Blender asset at `res://art/customtable_9ft.blend`, configured Godot to use `/usr/local/bin/blender`, and verified the direct `.blend` import path with Godot 4.6 Mono. The import generated the expected `.blend` import metadata plus extracted textures under `godot/art/textures/`, with `dotnet test` still at `38/38`, `dotnet build` still passing for the adapter, `--build-solutions` completing through the Blender import, and headless startup still exiting cleanly via `--quit-after 10`.
- 2026-03-14: Step 14 continued. Kept freeplay human-only, renamed the user-facing training labels to `FreePlay`, and added a simple 8-ball computer opponent that takes Player 2 turns by simulating candidate legal shots against cloned portable worlds. Reverified with `dotnet test` for `38/38` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Step 14 continued. Refined previously simplified physics in the portable core by adding moving side-spin scrub and modest cloth drift, capped tangential spin transfer in ball-ball collisions, and tangential rail response in boundary contacts. Added standalone tests for cloth drift, spin-aware collision transfer, and rail english, updated the straight-shot replay fingerprint to `d317d81f59530acc884987a9869805047203f4be969aa7034287c78a8e3eff6f`, and reverified with `dotnet test` for `41/41` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Step 14 continued. Made Godot debug mode writable by adding runtime tuning controls for slide friction, roll friction, spin decay, side-spin curve/scrub, ball/rail restitution, tangential transfer, settle threshold, and solver iteration counts. The adapter now rebuilds `SimulationWorld` with the current ball state after each tuning change and surfaces the selected parameter plus controls directly in the HUD/debug panel. Reverified with `dotnet test` for `41/41` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Step 14 continued. Reworked the Godot HUD into clearer cards by adding a dedicated shot-setup panel with speed/tip/tuning visuals, a separate controls/help panel toggled by `F6`, and a leaner status panel that no longer has to dump the entire control map as text. Reverified with `dotnet test` for `41/41` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Step 14 continued. Added a real start/menu overlay to the Godot adapter with button-driven `EightBall` and `FreePlay` selection, plus `Esc` reopen, resume, reset, and return-to-start actions so mode selection is no longer primarily a hidden hotkey flow. Reverified with `dotnet test` for `41/41` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Step 14 continued. Replaced the single-radius pocket capture rule with a mouth/drop model derived from hardcoded jaw geometry, so pocket acceptance now depends on lane and entry speed. Added direct resolver tests for deep-drop capture, controlled mouth capture, high-speed skim rejection, and outside-lane rejection, and reverified with `dotnet test` for `45/45` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Step 14 continued. Refined base rail rebound feel by separating glancing and head-on rail restitution, preserving most tangential bank travel through a dedicated retention term, and preventing overlap correction from applying fake rail scrub to balls already moving away from the cushion. Added standalone tests for glancing-bank tangent retention, glancing-versus-head-on normal rebound, and outgoing-overlap correction, exposed the new rail tuning fields in the Godot debug tuner, updated the straight-shot replay fingerprint to `eefdb8c9320f16e02ca261af596cc6910d2487f324b508363a3b11433107e9a3`, and reverified with `dotnet test` for `48/48` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Step 14 continued. Strengthened english on rails by separating spin-driven rail throw from base rail friction, so opposite side spin now produces substantially different cushion outcomes instead of only small tangent nudges. Added standalone tests for opposite-english rail throw and running-versus-check english on the same bank, exposed the new rail-english transfer field in the Godot debug tuner, and reverified with `dotnet test` for `50/50` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Step 14 continued. Added controlled follow/draw carry-through on ball-object contact by converting part of the cue ball's forward/back spin into immediate post-contact motion, while fixing the zero-friction cloth path so collision tests no longer auto-generate perfect roll before impact. Added standalone tests for follow-through and draw-back on head-on contact, exposed the new follow/draw carry field in the Godot debug tuner, updated the straight-shot replay fingerprint to `3472b836de60b73f322915a02f66dad115f7c47c4331ca3811e362ad63538d0f`, and reverified with `dotnet test` for `52/52` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Step 14 continued. Tightened pocket feel with a shelf-like slow lip-hang rule so very slow edge balls can stay up while equally slow center-line rollers still fall. Added direct resolver tests for slow lip hang and slow center-line capture, and reverified with `dotnet test` for `54/54` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Step 14 continued. Tuned the Godot shot-power envelope so regular shots now clamp to `0.3-5.0 m/s`, eight-ball break shots clamp to `0.3-8.0 m/s`, the HUD/debug readouts show the active cap, and the computer opponent now uses separate speed samples plus a harder target speed on the break. Reverified with `dotnet test` for `54/54` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Step 14 continued. Corrected the top-left pocket reference from `pockett_TL1` to `pocket_TL1` across code and docs, stopped boundary and ball overlap correction from emitting fake rules-layer contact events, made simultaneous cue-ball contact tie-breaks stable by sorting contact pairs before publishing `FirstContact`, and added a Godot-side computer-turn failover that hands the opponent ball in hand instead of retrying the same broken CPU turn forever. Added standalone regression tests for separating-ball overlap correction, stable simultaneous first-contact selection, and non-impact rail overlap correction, then reverified with `dotnet test` for `56/56` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Source sync continued. Re-read the corrected live Blender scene and updated the top-left pocket seed to the current `pocket_TL1` origin `(1.2491170, 0.6178464)` in both `CustomTable9FtSpec` and the reference markdowns. Reconfirmed the Godot-side verification with Godot 4.6 Mono `--build-solutions` and a clean headless startup run via `--quit-after 10`; the current validated code state remains `56/56` passing standalone tests and a successful `dotnet build` for the Godot adapter.
- 2026-03-14: Step 14 continued. Added a full gameplay-HUD toggle in the Godot adapter so `F7` now hides or restores the status, shot-summary, shot-setup, controls/help, debug, and shot-banner UI without affecting the start/pause menu overlay. Reverified with `dotnet test` for `56/56` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Source sync continued. Resaved the Blender source, updated the hardcoded `pocket_BM3` bottom-middle pocket seed to `(0.0000029, -0.6614339)`, renamed stale Blender mesh-data blocks so Godot now imports `pocket_TL1` and `rail_upper_left` instead of old typo names, removed temporary `customtable_9ft_fixed` import leftovers from the Godot project, and reverified with `dotnet test` for `56/56` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Step 14 continued. Set the Godot adapter to open its playable window at `1920x1080` by default through `DisplayServer.WindowSetSize()`, leaving headless validation unchanged. Reverified with `dotnet test` for `56/56` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 10`.
- 2026-03-14: Step 14 continued. Switched the Godot adapter to require the authored Blender `CueBall` plus `Ball_01` through `Ball_15` meshes instead of spawning fallback spheres, started using the imported Blender cue stick during shot setup, and added visible ball-roll accumulation from mirrored travel plus side-spin yaw so play no longer reads as sliding decals. Reverified with `dotnet test` for `56/56` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions`, and a clean headless startup run via `--quit-after 5`.
- 2026-03-14: Source sync continued. Re-copied `/home/justin/Desktop/customtable_9ft.blend` into the Godot project, removed stale custom split normals from `Tableslate`, triangulated `Tableslate`, `Tableframe`, `CueStick`, and `rail_upper_right` on the Godot project copy so tangent generation no longer fails during import, and removed an accidental `Ball_02` per-scene override from `Main.tscn`. Reverified with `dotnet test` for `56/56` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono repeated `--build-solutions` passes, and a clean headless startup run via `--quit-after 5`.
- 2026-03-14: Step 14 continued. Moved the Godot debug readout into a detached play-mode `Window` so it can live on a second monitor, rewrote the debug text into clearer grouped sections, added shot-power percentage to the shot-setup HUD, switched all imported ball textures to lossless non-mipmapped imports, and tightened desktop render sharpness by forcing native 3D scale with TAA and screen-space AA disabled plus `MSAA 3D` enabled. Fixed a bad `CloseRequested` disconnect in the new debug-window wiring after it showed up in a headless startup pass, then reverified with `dotnet test` for `56/56` passing standalone tests, `dotnet build` for the Godot adapter, Godot 4.6 Mono `--build-solutions --quit`, and a clean headless startup run via `--quit-after 5`.
